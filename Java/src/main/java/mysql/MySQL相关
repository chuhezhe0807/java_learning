SQL语句分类
DDL：数据定义语句【create 表，库...】
DML：数据操作语句【增加 insert，修改 update, 删除 delete】
DQL：数据查询语句【select】
DCL：数据控制语句【管理数据库：比如用户权限 grant(授权) revoke(撤销权限)】

MySQL数据类型
    数值类型（分有符号和无符号，默认有符号 例如：无符号int表数范围[0~2^32 - 1]，有符号[-2^31~2^31-1]）
        整数（bit(M) M指定位数 默认值1 范围1~64 tinyint(1个字节)、smallint（2个字节）、mediumint（3个字节）、int（4个字节）和bigint（8个字节））
        小数类型（float（单精度 4个字节）、double（双精度 8个字节）和decimal[M, D] 大小不确定 M 指定长度，D精确到的小数位数）
            decimal[M, D] [unsigned] M 为精度，表示该值的总长度，范围1~65 默认值10 D为标度，表示小数点后面的长度，范围是0~30 默认值0
    文本类型（字符串类型）
        char(0~255)、varchar(0~65535) [0~2^16 - 1]、text(0~2^16 - 1) 和 longtext(0~2^32 - 1)
            char(size) 固定长度字符串 最大255字符
                char(4) 这个4表示字符数(最大255)，不管是中文还是字母都是放四个，按字符计算
                char(4) 是定长，也就是说，即使插入 'aa'，也会占用分哦欸的4个字符的空间
            varchar(size) 可变长度字符串 最大65532字节【utf8编码最大21844字符，1-3个字节用于记录大小】（utf8编码三个字节表示一个字符，gbk两个字节表示一个字符）
                varchar(4) 这个4表示字符数，不管是字母还是中文都以定义好的表的编码来存放数据
                varchar(4) 是变长，也就是说，插入 'aa' 实际占用的空间大小并不是4个字符，而是按照实际占用来分配，varchar本身还需要占用1-3个字节来记录存放内容长度
            varchar和char占用的空间都和编码格式有关，如果编码为utf8则能存放4 * 3个字节，gbk则是4 * 2个字节
            如果数据是定长的，推荐使用char，如md5的密码，邮编，手机号char(13)，身份证号等，不定长则使用varchar
            查询速度：char > varchar
    二进制数据类型
        blob [0~2^16 - 1]、longblob [0~2^32 - 1]
    日期类型
        date [日期 年月日]、time [时间 时分秒]、timestamp [时间戳]和 datetime [年月日 时分秒 YYYY-MM-DD HH:mm:ss]

SQL语句
    创建数据库 create db_name [create_specification [, create_specification]...]
    create_specification:
        [DEFAULT] CHARACTER SET charset_name // 指定数据采用的字符集，默认utf8
        [DEFAULT] COLLATE collation_name // 指定数据字符集的校对规则（常用：utf8_bin[区分大小写] utf8_general_ci[不区分大小写，同时也是默认值]）

    创建表
        CREATE TABLE table_name (
            field1 datatype,
            field2 datatype,
            field3 datatype,
        ) character set 字符集 collate 校对规则 engine 存储引擎
        field 指定列名 datatype 指定列的类型
        character set 如不指定则为所在数据库字符集
        collate 如不指定则为所在数据校对规则

    修改表
        添加列 ALTER TABLE table_name ADD (column datatype [DEFAULT expr] [, column datatype]...)
        修改列 ALTER TABLE table_name MODIFY (column datatype [DEFAULT expr] [, column datatype]...)
        删除列 ALTER TABLE table_name DROP (column)
        查看表的结构 DESC table_name -- 可以查看表的所有的列
        修改表名 RENAME TABLE table_name1 TO table_name2
        修改表字符集 ALTER TABLE table_name CHARACTER SET 字符集

    修改记录
        UPDATE tablename SET column1 = new_column1, column2 = new_column2 [, column3 = new_column3...] WHERE condition1 AND condition2; (也可以使用 ... WHERE condition1 OR condition2)

    查询记录
        SELECT [DISTINCT] *|[column1 | expression, column2 | expression, column3 | expression...] FROM table_name;
        column 指定列名，* 代表查询所有列，FROM 指定查询那张表，DISTINCT 可选，指显示结果时，是否去掉重复数据
        SELECT column_name as 别名 FROM 表名; -- 可以取别名

        使用 order by 子句排序查询结果 asc 升序，desc 降序
        SELECT column1, column2, column3... FROM table_name; order by column asc|desc, ...

    合计/统计函数 - count、sum
        count 返回行的总数 SELECT count(*) | count([DISTINCT] column) from table_name [where condition]
        count(*) 返回满足条件的记录有多少行 count(列) 返回满足条件的列的记录有多少行，但是会排除为null的记录

        sum 返回满足条件的行的和，一般是用在数值列
        SELECT SUM(列名) {, SUM(列名)...} FROM table_name [WHERE condition]

        avg 返回满足where条件的一列的平均值
        SELECT avg(列名) {, avg(列名)...} FROM table_name [WHERE condition]

        max 返回最大值 min 返回最小值

    group by 子句对列进行分组，用于对查询的结果分组统计
        SELECT column1, column2, column3... FROM table_name GROUP BY column;

    having 子句对分组后的结果进行过滤，用于限制分组显示结果
        SELECT column1, column2, column3... FROM table_name GROUP BY column HAVING 。。。。

    分页查询
        基本语法 select ... limit start, rows 表示从start+1行开始取，去除rows行，start从0开始计算。
        select ... limit 每页显示记录数 * （第几页 - 1）， 每页显示的记录数

    如果select语句同时包含有group by，having，limit，order by 那么他们的顺序是
        SELECT column1, column2, column3... FROM table_name
            GROUP BY column
            HAVING condition
            ORDER BY column
            LIMIT start, rows;


    多表查询
    SELECT * FROM table1, table2;
    在默认情况下，当两个表查询时，
        1、从第一张表中，取出一行和第二张表的每一行进行组合，返回结果是含有两张表的所有列。
        2、一共返回的记录数为第一张表行数*第二张表的行数
        3、这样多表查询默认处理返回的结果，成为笛卡尔集
        4、解决多表的关键就是要正确写出过滤条件，WHERE
    注意多表查询时，条件不能少于表的个数-1，否则会出现笛卡尔集

    自连接
        自连接是指在同一张表的连接查询（将同一张表看作两张表） 注意自连接的表一定要起别名 表名 表名别名
        SELECT * FROM table1 table1_alias1, table1 table1_alias2;

    子查询
        子查询是指嵌入在其他sql语句中的select语句，也结交嵌套查询
        单行子查询是指只返回一行数据的子查询语句
        多行子查询是指返回多行数据的子查询，使用关键字in

    多列子查询
        多列子查询是指查询返回多个列数据的子查询语句

    合并查询
        为了合并多个select语句的结果，可以使用集合操作符号 union, union all

        union all 该操作符用于取得两个结果的并集，当使用该操作符时，不会取消重复行
        select ename, job, sal from emp where sal > 2500 union all
        select ename, job, sal from emp where job = 'MANAGER';