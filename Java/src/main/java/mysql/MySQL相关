SQL语句分类
DDL：数据定义语句【create 表，库...】
DML：数据操作语句【增加 insert，修改 update, 删除 delete】
DQL：数据查询语句【select】
DCL：数据控制语句【管理数据库：比如用户权限 grant(授权) revoke(撤销权限)】

MySQL数据类型
    数值类型（分有符号和无符号，默认有符号 例如：无符号int表数范围[0~2^32 - 1]，有符号[-2^31~2^31-1]）
        整数（bit(M) M指定位数 默认值1 范围1~64 tinyint(1个字节)、smallint（2个字节）、mediumint（3个字节）、int（4个字节）和bigint（8个字节））
        小数类型（float（单精度 4个字节）、double（双精度 8个字节）和decimal[M, D] 大小不确定 M 指定长度，D精确到的小数位数）
            decimal[M, D] [unsigned] M 为精度，表示该值的总长度，范围1~65 默认值10 D为标度，表示小数点后面的长度，范围是0~30 默认值0
    文本类型（字符串类型）
        char(0~255)、varchar(0~65535) [0~2^16 - 1]、text(0~2^16 - 1) 和 longtext(0~2^32 - 1)
            char(size) 固定长度字符串 最大255字符
                char(4) 这个4表示字符数(最大255)，不管是中文还是字母都是放四个，按字符计算
                char(4) 是定长，也就是说，即使插入 'aa'，也会占用分哦欸的4个字符的空间
            varchar(size) 可变长度字符串 最大65532字节【utf8编码最大21844字符，1-3个字节用于记录大小】（utf8编码三个字节表示一个字符，gbk两个字节表示一个字符）
                varchar(4) 这个4表示字符数，不管是字母还是中文都以定义好的表的编码来存放数据
                varchar(4) 是变长，也就是说，插入 'aa' 实际占用的空间大小并不是4个字符，而是按照实际占用来分配，varchar本身还需要占用1-3个字节来记录存放内容长度
            varchar和char占用的空间都和编码格式有关，如果编码为utf8则能存放4 * 3个字节，gbk则是4 * 2个字节
            如果数据是定长的，推荐使用char，如md5的密码，邮编，手机号char(13)，身份证号等，不定长则使用varchar
            查询速度：char > varchar
    二进制数据类型
        blob [0~2^16 - 1]、longblob [0~2^32 - 1]
    日期类型
        date [日期 年月日]、time [时间 时分秒]、timestamp [时间戳]和 datetime [年月日 时分秒 YYYY-MM-DD HH:mm:ss]

SQL语句
    创建数据库 create db_name [create_specification [, create_specification]...]
    create_specification:
        [DEFAULT] CHARACTER SET charset_name // 指定数据采用的字符集，默认utf8
        [DEFAULT] COLLATE collation_name // 指定数据字符集的校对规则（常用：utf8_bin[区分大小写] utf8_general_ci[不区分大小写，同时也是默认值]）

    创建表
        CREATE TABLE table_name (
            field1 datatype,
            field2 datatype,
            field3 datatype,
        ) character set 字符集 collate 校对规则 engine 存储引擎
        field 指定列名 datatype 指定列的类型
        character set 如不指定则为所在数据库字符集
        collate 如不指定则为所在数据校对规则

    修改表
        添加列 ALTER TABLE table_name ADD (column datatype [DEFAULT expr] [, column datatype]...)
        修改列 ALTER TABLE table_name MODIFY (column datatype [DEFAULT expr] [, column datatype]...)
        删除列 ALTER TABLE table_name DROP (column)
        查看表的结构 DESC table_name -- 可以查看表的所有的列
        修改表名 RENAME TABLE table_name1 TO table_name2
        修改表字符集 ALTER TABLE table_name CHARACTER SET 字符集

    修改记录
        UPDATE tablename SET column1 = new_column1, column2 = new_column2 [, column3 = new_column3...] WHERE condition1 AND condition2; (也可以使用 ... WHERE condition1 OR condition2)

    查询记录
        SELECT [DISTINCT] *|[column1 | expression, column2 | expression, column3 | expression...] FROM table_name;
        column 指定列名，* 代表查询所有列，FROM 指定查询那张表，DISTINCT 可选，指显示结果时，是否去掉重复数据
        SELECT column_name as 别名 FROM 表名; -- 可以取别名

        使用 order by 子句排序查询结果 asc 升序，desc 降序
        SELECT column1, column2, column3... FROM table_name; order by column asc|desc, ...

    合计/统计函数 - count、sum
        count 返回行的总数 SELECT count(*) | count([DISTINCT] column) from table_name [where condition]
        count(*) 返回满足条件的记录有多少行 count(列) 返回满足条件的列的记录有多少行，但是会排除为null的记录

        sum 返回满足条件的行的和，一般是用在数值列
        SELECT SUM(列名) {, SUM(列名)...} FROM table_name [WHERE condition]

        avg 返回满足where条件的一列的平均值
        SELECT avg(列名) {, avg(列名)...} FROM table_name [WHERE condition]

        max 返回最大值 min 返回最小值

    group by 子句对列进行分组，用于对查询的结果分组统计
        SELECT column1, column2, column3... FROM table_name GROUP BY column;

    having 子句对分组后的结果进行过滤，用于限制分组显示结果
        SELECT column1, column2, column3... FROM table_name GROUP BY column HAVING 。。。。

    分页查询
        基本语法 select ... limit start, rows 表示从start+1行开始取，去除rows行，start从0开始计算。
        select ... limit 每页显示记录数 * （第几页 - 1）， 每页显示的记录数

    如果select语句同时包含有group by，having，limit，order by 那么他们的顺序是
        SELECT column1, column2, column3... FROM table_name
            GROUP BY column
            HAVING condition
            ORDER BY column
            LIMIT start, rows;


    多表查询
    SELECT * FROM table1, table2;
    在默认情况下，当两个表查询时，
        1、从第一张表中，取出一行和第二张表的每一行进行组合，返回结果是含有两张表的所有列。
        2、一共返回的记录数为第一张表行数*第二张表的行数
        3、这样多表查询默认处理返回的结果，成为笛卡尔集
        4、解决多表的关键就是要正确写出过滤条件，WHERE
    注意多表查询时，条件不能少于表的个数-1，否则会出现笛卡尔集

    自连接
        自连接是指在同一张表的连接查询（将同一张表看作两张表） 注意自连接的表一定要起别名 表名 表名别名
        SELECT * FROM table1 table1_alias1, table1 table1_alias2;

    子查询
        子查询是指嵌入在其他sql语句中的select语句，也结交嵌套查询
        单行子查询是指只返回一行数据的子查询语句
        多行子查询是指返回多行数据的子查询，使用关键字in

    多列子查询
        多列子查询是指查询返回多个列数据的子查询语句

    合并查询
        为了合并多个select语句的结果，可以使用集合操作符号 union, union all

        union all 该操作符用于取得两个结果的并集，当使用该操作符时，不会取消重复行
        select ename, job, sal from emp where sal > 2500 union all
        select ename, job, sal from emp where job = 'MANAGER';

    外连接（分为左外连接和右外连接）
        外连接的必要性：
            多表查询是通过利用where字句对两张表或多张表，形成笛卡尔集进行筛选，根据关联条件，显示所有匹配的记录，匹配不上的不显示，
            所以就有可能出现关联条件（关联列）数据不一起完全对的上的情况，比如employee表和dept表多表查询时使用deptno字段作为关联条件
            有可能dept表的deptno有40这条记录，但是employee整张表中都没有deptno为40的记录，这样多表查询就会有匹配不上漏掉的字段
        左外连接 如果左侧的表完全显示我们就说是左外连接 SELECT ... FROM 表1 LEFT JOIN 表2 ON [condition] (表1就是左表，因为他在左边)
        右外连接 如果右侧的表完全显示我们就说是右外连接 SELECT ... FROM 表1 RIGHT JOIN 表2 ON [condition] (表1就是左表，因为他在左边)

    约束
        约束用于确保数据库的数据满足特定的商业规则，在mysql中，约束包括not null, unique, primary key, foreign key 和 check五种
        primary key 主键
            基本使用：字段名 字段类型 primary key
            用于唯一的标识表行的数据，当定义主键约束后，该列的数据不能重复
            · primary key不能重复而且不能为null 默认就是不能为null
            · 一张表最多只能有一个主键，但是可以是复合主键
            · 主键的定义方式：1、创建表时 字段名 primary key 2、在表定义最后面写 primary key (列名) create table `table_name` (id int, ..., PRIMARY KEY(id));
            · 使用 DESC 表名，可以看到primary key的情况
        not null 非空
            基本使用：字段名 字段类型 NOT NULL
        unique 唯一
            基本使用：字段名 字段类型 unique
            当定义了唯一约束后，该列的值是不能重复的
            · 如果没有指定not null，则unique字段可以有多个null
            · 一张表可以有多个unique字段
        foreign key 外键
            用于定义主表和从表之间的关系：外键约束要定义在从表上，主表则必须具有主键约束或是unique约束，当定义外键约束后，
            要求外键列数据必须在主表的主键列存在或是为null
            基本使用：FOREIGN KEY (本表字段名) REFERENCES 主表名(主键名或unique字段名)
            · 外键指向的表的字段，要求是primary key 或者是unique
            · 表的类型时innodb，这样的表才支持外键
            · 外键字段的类型要和主键字段的类型一致（长度可以不同）
            · 外键字段的值，必须在主键字段中出现过，或者为null（前提是外键字段允许为null）
            · 一旦建立主外键关系，数据就不能随意删除了
        check 检查约束
            基本使用：CHECK <表达式> 例如：`sex` char(1) CHECK (`sex` IN ('男', '女')),
            mysql5.7 还不支持check，只做语法校验，但不会生效

    自增长 AUTO_INCREMENT
        · 一般来说自增长适合primary key配合使用的
        · 自增长也可以单独使用，但是需要配合一个unique
        · 自增长修饰的字段为整数型的（虽然小数也可以，但是很少用）
        · 自增默认从1开始，也可以通过如下命令修改
            ALTER 表名 AUTO_INCREMENT = XXX;
        · 如果添加记录时，制定了自增长字段的值，那么以这个值为准，如果指定了自增长就不要再给值了

    索引
        基本用法：CREATE INDEX 索引名称 ON 表名 (列名) // 表示在表的一列上创建索引
        创建索引后，只对创建了索引的列有效，查询速度提升十分可观，但是会增大表占用的磁盘空间
        没有索引为什么会慢？因为全表扫描
        使用所以为什么会快？形成一个索引的数据结构，比如二叉树
        索引的代价：1、磁盘占用 2、对dml(update, delete, insert) 语句的效率影响(二叉树需要重新平衡等其他索引维护操作)
        索引的类型：
            1、主键索引，主键自动的为主索引（类型Primary）
            2、唯一索引（UNIQUE） 如果某列是不会重复的，优先使用unique索引，否则使用普通索引
            3、普通索引（INDEX）
            4、全文索引（FULLTEXT）[适用于MyISAM] mysql自带的全文索引效率比较低，开发中考虑使用全文搜索Solr和ElasticSearch(ES)
        哪些列上面适合建立索引
            1、较为频繁的作为查询条件的字段应该创建索引
            2、唯一性太差的字段不适合单独创建索引 例如：select * from table_name where sex = '男' sex列就不适合创建索引
            3、更新非常频繁的字段不适合创建索引
            4、不会出现在where子句中的字段不应该创建索引


    事务
        事务用于保证数据的一致性，它由一组相关的dml语句组成，改组的dml语句要么全部成功，要么全部失败。
        当执行事务操作时（dml语句），mysql会在表上加锁，防止其他用户该表的数据，这对用户来讲是非常重要的
        保存点：
            保存点是事务中的点，用于取消部分事务。
        回退事务：
            当结束时事务(commit)时，通过指定保存点可以回退到指定的点，会自动地删除该事务所定义的保存点，当执行回退事务时，
            通过指定保存点可以回退到指定的点。回退到指定的保存点后，会将指定的保存点到回退起始位置之间的所有保存点都删掉(指定的保存点不删除)。
        提交事务：
            使用commit语句可以提交事务，当执行了commit语句后，会确认事物的变化、结束事务、删除保存点、释放锁、数据生效。
            当使用commit语句结束事务后，其他会话将可以查看到事务变化后的新数据，所有数据正式生效。
        注意事项：
            1、如果不开始事务，默认情况下，dml操作时自动提交的，不能回滚
            2、如果开始一个事务，但是没有创建保存点，也可以执行rollback，默认就是回到事务开始的状态
            3、可以在事务没有提交前，选择回退到哪个保存点
            4、mysql的事务机制需要innodb的存储引擎
            5、BEGIN; 或 START TRANSACTION; 来开启事务。
        事务隔离级别介绍(在开启事务后，单次事务过程中，除本次事务操作外，所有的数据应该保持事务开始时的状态)
            1、多个连接开启事务操作数据库中的数据时，数据库系统要负责隔离操作，以保证各个连接在获取数据时的准确性。
            2、如果不考虑隔离性，可能会引发如下问题：
                脏读：当一个事务读取另一个事务尚未提交的改变（添加、修改或删除）时，产生脏读
                不可重复读：同一查询在同一事务中多次进行，由于其他提交事务所做的修改或删除，每次返回不同的结果集，此时发生不可重复读
                幻读：同一查询在同一事务中多次进行，由于其他提交事务所做的插入操作，每次返回不同的结果集，此时发生幻读
            MySQL默认的事务隔离级别是 repeatable read (可重复读)，一般情况下，没有必要修改，该级别可以满足绝大部分项目需求。
            可以修改my.ini配置文件，实现全局配置事务隔离级别
                [mysqld]
                transaction-isolation = REPEATABLE-READ; (可选 READ-UNCOMMITTED, READ-COMMITTED, REPEATABLE-READ, SERIALIZABLE)

        事务ACID特性
            原子性（Atomicity）
                原子性是指事务是一个不可分割的工作单位，事务中的操作要么都发生，要么都不发生。
            一致性（Consistency）
                事务必须使数据库从一个一致性状态变换到另外一个一致性状态。
            隔离性（Isolation）
                事物的隔离性是多个用户并发访问数据库时，数据库为每一个用户开启的事务，不能被其他事务的操作数据所干扰，多个并发事务之间要相互隔离。
            持久性（Durability）
                持久性是指一个事务一旦被提交，他对数据库中数据的改变就是永久性的，接下来即使数据库发生故障也不应该对其有任何的影响。


    mysql表类型和存储引擎
        mysql的表类型由存储引擎（Storage Engines）决定，主要包括MyISAM、innDB、Memory等
            MyISAM不支持事务，也不支持外键，但其访问速度快，对事务完整性没有要求
            InnoDB存储引擎提供了具有提交、回滚和崩溃恢复能力的事务机制。但是比起MyISAM存储引擎，InnoDB写的处理效率差一些并且会占用更多的磁盘空间以保留数据和索引。
            MEMORY存储引擎使用存在内存中的内容来创建表。每个MEMORY表知识及对应一个磁盘文件。MEMORY类型的表访问非常快，
                因为它的数据是放在内存中的，并且默认使用hash索引。但是一旦服务关闭，表中的数据就会丢失掉，表的结构还在。

        mysql数据表主要支持六种类型，分别是：CSV、Memory、ARCHIVE、MRG_MYISAM、MYISAM、InnoDBB



